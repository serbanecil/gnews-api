# Simple API to retrieve the news from 'GNEWS'.

Inside the application.properties configure the property `apiKey`

The swagger-ui can be accessed under: http://localhost:8080/news/swagger-ui/index.html

